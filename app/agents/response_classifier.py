# (stub) reserved for real reply classification
# Reserved for real reply classification (IMAP + LLM). Not used in this MVP.
def predict_category_from_reply(reply_text: str) -> str:
    return "interested"
